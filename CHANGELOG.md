# 更新日志

### 2026-04-24

1. 新增同行自动确认：非战斗状态下检测到 `qiudaidai.png`（画面中心区域）时静默按 `F` 确认重新建立同行。
2. 战斗开始日志显示累计污染次数。
3. 模板分类重构：行动检测排除 `qiudaidai.png` 和 `yes.png`，明确划分模板用途。
4. 移除 `chat.png` 依赖，战斗开始改为非战斗状态下行动检测直接触发。

### 2026-04-23

1. 战斗区间检测重构：战斗结束由 `elf_P.png` 等模板识别触发，职责分离更清晰。
2. 终端新增战斗分界线：每场战斗开始和结束时输出分界线，直观区分不同战斗的消息区间。
3. 智能模式升级为可配置模式：运行前可选择是否自定义污染/普通战斗的行为。
4. 新增第三种行为选项"释放技能1后聚能"：首次按 `1`，后续按 `X`。
5. 三种可选行为：只聚能（按 `X`）、逃跑（`ESC` + 确认）、释放技能1后聚能。

### 2026-04-20

1. 模块化重构：将单体 `main.py`（665行）拆分为 `core/` 和 `modes/` 两个包，职责分离，便于维护。
2. 引入策略模式（`BaseMode` 抽象基类），新增功能只需创建一个模式类文件并在注册表中添加一行。
3. 移除不可用的 F8 热键退出机制，统一使用 Ctrl+C 退出。
4. 移除 `keyboard` 库依赖。

### 2026-04-17

1. 新增污染计数功能，方便刷异色的时候统计保底。同时增加单独的污染计数模式，不进行任何键鼠模拟操作，供抓异色的玩家后台计数。
2. 战斗次数统计逻辑基于行动检测的上升沿（前一轮未检测到、后一轮检测到）进行 +1 计数，避免重复累计。
3. 新增智能模式（Mode 4）：在每场战斗开始时进行一次类型判定，污染战斗执行聚能（按 `X`），普通战斗执行逃跑（`ESC` + 点击确认）。
4. 新增污染判型模板：支持通过 `capture.png` 与 `pollute_capture.png` 的匹配分数比较区分战斗类型；`pollute_capture` 分数更高判为污染战斗。
5. 终端状态输出精简：移除聊天检测/聊天分数字段显示，保留核心行动状态与污染计数，减少刷屏干扰。
6. 终端输出中文化：关键状态字段与提示文案改为中文，观察和排查更直观。
7. 审计日志中文化并精简：`logs/audit.log` 仅保留便于回顾的关键事件，减少噪声信息。

### 2026-04-16

1. 分辨率支持升级：脚本已支持自适应分辨率，推荐 2K（2560x1600 或 2560x1440）以获得更高识别率。
2. 低分辨率兼容优化：默认匹配阈值下调至 0.5，低分辨率下更容易触发识别。
3. 后台识别增强：窗口被遮挡时仍可检测（窗口不要最小化）。
4. 逃跑模式点击策略简化：统一使用物理鼠标点击，稳定性更高。
5. 可自定义适配：若某些分辨率识别不稳定，可按当前分辨率重截 `templates` 中对应图片替换。
6. 排查更方便：新增 `logs/audit.log` 审计日志，方便定位"为什么触发/为什么没触发"。
7. README 中加入建议用法。
